
# TDD開発環境
node_modules/
src/generated/
coverage/
.env
.DS_Store
Thumbs.db

# TypeScript build output (keep dist/ for CDN usage)
*.tsbuildinfo

# コールグラフ
codeql-db

# secrets
.secrets

# Rust
rust/target/
rust/Cargo.lock

# WASM pkg is needed for library usage, so not ignored

# Tree-sitter generated files (keep parser.c and .wasm)
tree-sitter-mml/node_modules/
tree-sitter-mml/bindings/
tree-sitter-mml/build/

# Added by cargo

/target
